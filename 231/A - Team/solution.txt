package main
 
import "fmt"
 
func main() {
	var n int
	fmt.Scan(&n)
	count := 0
	for i := 0; i < n; i++ {
		var petya , vasya ,tonya bool
		fmt.Scan(&petya, &vasya, &tonya)
		if (petya && vasya) || (petya && tonya) || (vasya && tonya)  {
			count++
		}
 
	}
	fmt.Println(count)
}