package main
 
import "fmt"
 
func main() {
	var n, h int
	fmt.Scan(&n, &h)
	height := make([]int, n)
	for i := 0; i < n; i++ {
		fmt.Scan(&height[i])
	}
	width := 0
	for i := 0; i < n; i++ {
		if height[i] > h {
			width += 2
		} else {
			width++
		}
	}
	fmt.Println(width)
}