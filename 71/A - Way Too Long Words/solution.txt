package main
 
import "fmt"
 
func main() {
	var n int
	fmt.Scan(&n)
 
	for i := 1; i <= n; i++ {
		var word string
		fmt.Scan(&word)
 
		if len(word) > 10 {
			fmt.Println(string(word[0]) + fmt.Sprint(len(word)-2) + string(word[len(word)-1]))
		} else {
			fmt.Println(word)
		}
	}
}