package main
 
import (
	"fmt"
	"strings"
)
 
func main() {
	var a, b string
	fmt.Scan(&a, &b)
	a = strings.ToLower(a)
	b = strings.ToLower(b)
	if a == b {
		fmt.Println("0")
	} else if a < b {
		fmt.Println("-1")
	} else {
		fmt.Println("1")
	}
}