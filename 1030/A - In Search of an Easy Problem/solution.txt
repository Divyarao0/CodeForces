package main
 
import "fmt"
 
func main() {
	var n int
	fmt.Scan(&n)
	m := make(map[int]bool)
	for i := 0; i < n; i++ {
		var value int
		fmt.Scan(&value)
		m[value] = true
	}
	if m[1] {
		fmt.Println("HARD")
	} else {
		fmt.Println("EASY")
	}
}