package main
 
import "fmt"
 
func main() {
	var k, n, w int
	fmt.Scan(&k, &n, &w)
	total := 0
	price := 0
	for i := 1; i <= w; i++ {
		price = k * i
		total += price
	}
 
	borrow := 0
	if total > n {
		borrow = total - n
	}
 
	fmt.Println(borrow)
}