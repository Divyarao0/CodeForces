package main
 
import "fmt"
 
func main() {
	var s string
	fmt.Scan(&s)
	cout := 0
	for i := 0; i < len(s); i++ {
		if s[i] == '4' || s[i] == '7' {
			cout++
		}
	}
	if cout == 4 || cout == 7 {
		fmt.Println("YES")
	} else {
		fmt.Println("NO")
	}
}